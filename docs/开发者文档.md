# 开发者文档

1. AI可以自主进行commit，一个short message示例:`docs: 分离需求描述文档`，AI提交的commit需要包含详细的message。

2. 项目的开发环境通过devcontainer定义，devcontainer内包括一个typescript主容器，一个redis容器用于bullmq。

3. 所有注释一律使用中文。

## 发布说明

项目发布一个npm包到npm官方镜像，和一个docker镜像发布到github，均通过workflow发布。

npm包需要更新 package.json 中的 version 才会发布到npm。